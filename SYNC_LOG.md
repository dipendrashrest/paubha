# Design System Sync Log

Autonomous overnight sync of `packages/registry` against the Figma source of truth (file `CDgfoMkj7lP3pXWJ3aOgkH`), started 2026-09-11. One line (or a few) per completed unit of work, in commit order. See the plan this run followed for full context.

_Summary block will be filled in at the top of this file once the final pass completes._

**⚠️ Two items need your decision before anything else in this log fully matters — both marked 🔴 in OPEN QUESTIONS below:**
1. **Brand/Gray/Success primitives** — Figma currently shows different values than the locked brand identity (a change was made, then reverted).
2. **Radius usage guidelines** — Figma's "Foundations / Radius" frame currently shows the exact buttons/inputs→md, modals→xl mapping that was deliberately corrected away from on 2026-08-23 (to sm/lg). Either Figma drifted back, or that correction misread the frame at the time.

Both are "Figma currently disagrees with a value CLAUDE.md documents as deliberately locked/corrected" situations — nothing tonight was built against the disputed values in either case (color tokens stayed on the locked palette; no component radius classes were touched), so nothing is blocked, but neither will be touched again autonomously until you decide which side is right.

## Noted for later (not tonight)

- Docs-site UI redesign (new "Docs · Desktop" Figma frame — top bar, sidebar nav, live-preview panels, props tables, pagination footer) — explicitly deferred to a separate, visually-reviewed session. When it happens: the frame's sample code (`@paubha/react`, `PaubhaProvider`) is illustrative, not literal — adapt to the real CLI install flow (`npx paubha init` / `add button`), no importable package or provider component unless separately requested.

## Phase 1 — Tokens

- **Info semantic tokens added (no dedicated primitive ramp — see OPEN QUESTIONS).** Pulled Foundations/Colors from Figma (`CDgfoMkj7lP3pXWJ3aOgkH`, frame "Foundations / Colors" 2066:14533 → "Status Colors" section, node 6180:9680) via `get_design_context`. Figma's "Status Colors" panel gives only 3 raw swatches for Info (fg/info, bg/info, border/info) — no 50–950 primitive ramp exists for Info in this file (only Brand, Neutral/Gray, and Success have full ramps). The 3 Info swatch hexes resolved to `#4469e5` (fg/info, border/info) and `#f0f4fe` (bg/info) — these are exactly Figma's own Brand/500 and Brand/50 swatch values (confirmed by cross-checking the "Brand Palette" frame, node 6180:1277), i.e. Figma's real design intent is that Info deliberately aliases the Brand hue rather than getting its own ramp. Added to `packages/registry/styles/tokens.css`: semantic tokens `--bg-info-solid`, `--bg-info-subtle`, `--fg-info`, `--fg-on-info`, `--border-info` in both the light (`:root`) and dark (`.dark`/`[data-preview-theme="dark"]`) blocks, bound via `var()` to the existing `--brand-*` primitives following the exact same relationship the codebase already uses for `--fg-brand`/`--border-brand`/`--bg-brand-subtle` (brand-600/brand-50 light, brand-400/brand-950 dark; `--fg-on-info` mirrors `--fg-on-brand`/`--fg-on-success` as `--base-white` in both modes). No new hex values were introduced — every info token is a `var()` alias of an already-confirmed real primitive. Mirrored the same 5 tokens into `packages/registry/styles/theme.css`'s `@theme inline` block (`--color-bg-info-solid`, `--color-bg-info-subtle`, `--color-fg-info`, `--color-fg-on-info`, `--color-border-info`), matching the existing warning/success mapping pattern. Did not add a primitive-layer `--color-info`/`--color-info-subtle` short alias (like `--color-error`/`--color-warning`/`--color-success`) since there is no `--info-*` primitive scale to alias.
- **Alert/Toast "info" variant wired to the new tokens (one-line swap each).** `packages/registry/ui/alert/alert.tsx`: `info` variant class changed from `"border-border-brand bg-bg-brand-subtle text-fg-brand"` to `"border-border-info bg-bg-info-subtle text-fg-info"` — functionally identical result today (since info tokens alias brand), but now uses the dedicated info semantic layer, matching how the `warning`/`success`/`error` cases already reference their own dedicated tokens. `packages/registry/ui/toast/toast.tsx`: `info` accent-bar class changed from `"bg-bg-brand-solid"` to `"bg-bg-info-solid"`, mirroring the `success`/`warning`/`error` cases exactly. Both are pure token-reference swaps — no markup, props, or variants changed. `pnpm build` and `pnpm test` both pass (306 registry tests + 17 CLI tests, no new failures) after these changes.

> **⚠️ REVERTED by the orchestrating pass (commit `344b794`) — do not treat the three bullets below as live in the codebase.** The investigation itself (which node ids, which values, the cross-checks) is left intact below because the research is real and useful, but the actual `tokens.css`/`CLAUDE.md` edits it produced were undone before continuing the overnight run. Reason: `CLAUDE.md`'s brand-identity section explicitly says `brand-600 = #2450EA` is locked — "do not deviate without being told" — specifically because it was the outcome of a deliberate, previously-litigated correction (indigo `#4658DE` → true blue `#2450EA`, with recorded contrast verification). Silently overwriting a value that's flagged that explicitly, however well-sourced the new number is, is exactly the kind of non-additive, cascades-into-everything change this run's own ground rules say to escalate rather than execute — brand-600 feeds every brand-solid surface, every brand-tinted shadow, and the focus ring across all 34 components. Code was restored to `brand-600 = #2450EA` (and the matching gray-200/gray-800/success-600 values) so the rest of tonight's component work builds on the known-locked palette, not a possibly-unintended Figma edit. **See the OPEN QUESTIONS entry "Brand/Gray/Success primitives: Figma shows different values than the locked brand identity" for the actual decision needed** — this is the single highest-priority item in this whole log; nothing about the new hex values below should be trusted until you've looked at it.
- **Brand ramp re-synced (full 11 steps) — resolved the brand/500 discrepancy flagged for this unit.** Investigated file `CDgfoMkj7lP3pXWJ3aOgkH` directly: `get_metadata`/`get_design_context` on the "Foundations / Colors" frame (`2066:14533`) returned the complete page in one subtree (height 4376 accounted for exactly by its children — header, Brand Palette, Neutral Palette, Success Palette, 3 Semantic Tokens sections, Status Colors, Dark Mode, footer), with no second/legacy "Brand Palette" or "Colors" frame anywhere else in the file. This is also the exact node `tokens.css`'s own header comment cites as its source, so it's the canonical page, not a stale duplicate. Its "Brand Palette" frame (`6180:1277`) reads `brand/500 = #4469E5`, matching the "Status Colors → Info" swatch (`fg/info`/`border/info` = `#4469E5`, `bg/info` = `#F0F4FE` = `brand/50`) on the *same* page — two independent swatches on one canonical frame agreeing exactly, which is as strong a confirmation as this task can get without a human. This is a real, additional drift beyond the documented 2026-08-22 correction (old code `brand-600 = #2450EA` "true blue" vs. this file's current `brand-600 = #2E4DD9`) — not a misread of a legacy frame and not the old pre-2026-08-22 indigo (`#4658DE`) reappearing; it's a third, distinct ramp that Figma has since moved to. Updated all 11 `--brand-*` steps in `packages/registry/styles/tokens.css` to match Figma exactly (50 `#F0F4FE` → 950 `#1A2150`), recomputed the two shadow-effect blocks that hardcode brand's RGB decomposition since plain CSS can't derive `rgb()` components from a hex custom property (`--shadow-xs..2xl`: brand-900 `rgb(30 52 133/…)` → `rgb(36 51 128/…)`; `--shadow-glow-focus`: brand-500 `rgb(59 99 245/…)` → `rgb(68 105 229/…)`), and updated `CLAUDE.md`'s brand-color callout and Design Tokens table to match, with a "Corrected 2026-09-11" note explaining the supersession. All other semantic/component code is `var()`-bound and picks this up automatically — no component files touched.
- **Gray (Neutral) and Success primitives spot-corrected — 3 total steps.** Same canonical page. "Neutral Palette" (`6180:1325`): 9 of 11 steps already matched code exactly; `neutral/200 = #EAECF0` (code had `#E4E7EC`) and `neutral/800 = #182230` (code had `#1D2939`) did not — updated `--gray-200` and `--gray-800` in `tokens.css` and `CLAUDE.md` to match. "Success Palette" (`6428:27293`): 10 of 11 steps matched; `success/600 = #058550` (code had `#079455`) did not — updated `--success-600` in both files. No structural changes (no tokens added/removed), values only, per scope.
- **Error and Warning primitive ramps: confirmed (again, independently) that no such frame exists in Figma — left untouched.** Walked the entire canonical "Foundations / Colors" frame (`2066:14533`) and its full child list (see above) — there is no "Error Palette" or "Warning Palette" 50–950 frame anywhere on this page, matching the previous unit's finding for Info. The only Error/Warning data in Figma is one semantic swatch each in "Status Colors" (`fg/error` = `#DC3A3A`, `fg/warning` = `#DC9517`), and these don't cleanly correspond to any single ramp step — worth noting, the Status Colors panel's `fg/success` swatch (`#249B5B`) doesn't even match the *current* Success Palette ramp's `success/600` (`#058550`) or the old code value (`#079455`) on this same page, which undermines treating Status Colors as authoritative for any full ramp. Left `--error-*` and `--warning-*` entirely as-is (not even the single semantic-swatch values), per the "leave code as-is rather than guess" rule. See OPEN QUESTIONS.
- **Resolved the "info" question from the previous unit: `#4469e5` was real and current, not a misread.** Confirmed above — it is Figma's live, current `brand/500`, verified via two independent swatches on the single canonical page. The previous agent's info-token work (aliasing `--fg-info`/`--bg-info-solid`/`--border-info`/etc. to the brand ramp rather than fabricating a 9-step info scale) remains the right call and needed no rework; it now automatically resolves to the corrected brand values via `var()`. One follow-up worth flagging: the previous unit's own code comment says info's swatches "resolve to the exact same hex values as brand/500 and brand/50," but the actual bindings use `var(--brand-600)` for `--fg-info`/`--bg-info-solid`/`--border-info` (not `--brand-500`) — i.e. the comment and the binding disagree on which brand step Info aliases. I did not change this binding myself since it's a semantic-layer design decision (which step to alias to), not a primitive-value fix, and outside this unit's scope — logged under OPEN QUESTIONS instead.
- Build (`pnpm build`) and test (`pnpm test`, 306 registry tests + 17 CLI tests) both pass with these changes. Committed `packages/registry/styles/tokens.css` and `CLAUDE.md` only (`apps/www/public/r/*.json` is a gitignored build artifact, regenerated automatically and not hand-edited). Docs-site files that hardcode literal copies of these hexes for illustration (`apps/www/content/docs/foundations/colors.mdx`, `apps/www/components/docs/_shared/foundation-scales.tsx`, `apps/www/components/docs/avatar/avatar-demos.tsx`, `apps/www/app/opengraph-image.tsx`) were intentionally left untouched — they're not token-bound, updating them is a docs-site content pass outside "touch only what this task requires" for a tokens-only unit, and doing so would have meant editing several component/content files beyond `tokens.css`/`CLAUDE.md`. Flagging so a later docs-sync phase picks them up — they'll show stale brand/gray/success swatches until then.
- **Added 2 of 3 requested missing semantic tokens; the 3rd (`bg-error-solid-active`) skipped — no Figma data exists for it (see OPEN QUESTIONS).** Loaded `figma-design-to-code`, then worked from the canonical "Foundations / Colors" frame (`2066:14533`, file `CDgfoMkj7lP3pXWJ3aOgkH`) per the previous units' confirmation that it's the sole such frame:
  - **`bg/overlay` → `--bg-overlay`.** Figma's "Semantic Tokens — Background" section (node `6180:9644`) lists `bg/overlay`, described as "Overlay backdrop — modal backgrounds at 60% opacity." Downloaded its swatch asset SVG directly (`get_design_context` returns swatches as `<img>` refs to exported SVGs; fetched the SVG and read its `fill`) — it's `#101828`, which is exactly the locked `--gray-900` primitive (confirmed, not touched). Figma shows no separate dark-mode swatch for this token anywhere reachable, so it's mode-invariant: added `--bg-overlay: rgb(16 24 40 / 0.6);` to both the light (`:root`) and dark (`.dark`) blocks in `tokens.css`, and `--color-bg-overlay: var(--bg-overlay);` in `theme.css`. This is a real correction, not a no-op: `packages/registry/ui/modal/modal.tsx` and `packages/registry/ui/dialog/dialog.tsx` both hardcoded the overlay as `bg-[var(--brand-900)]/60` (brand-tinted), but Figma's actual spec is gray/neutral-tinted, not brand-tinted. Updated both files' `Overlay` className from `"fixed inset-0 z-50 bg-[var(--brand-900)]/60"` to `"fixed inset-0 z-50 bg-bg-overlay"` (opacity is now baked into the token itself, so no `/60` suffix needed on the utility class). Checked `dropdown-menu`, `select`, `popover` per the task's instruction — none of them have their own overlay/backdrop; only Modal and Dialog use one.
  - **`focus/ring-error` → `--shadow-glow-focus-error`.** Confirmed via `search_design_system` (entity: style, query "focus glow effect") that this file has no local effect style for an error-tinted focus ring at all — every result returned was from unrelated external community libraries ("Website/Kiosk/Mobile Design System"), not this file's own assets. Since CLAUDE.md documents the *shape* of `--shadow-glow-focus` explicitly and exactly (4px spread, 24% opacity, zero blur/offset, brand/500 as the color), and that shape is itself the locked, non-negotiable brand signature, I derived `--shadow-glow-focus-error` by applying that identical formula to the already-locked `--error-500` primitive (`#f04438` = `rgb(240 68 56)`) instead of brand/500: `--shadow-glow-focus-error: 0 0 0 4px rgb(240 68 56 / 0.24);`. Added to `tokens.css` right after `--shadow-glow-focus` (single definition, not duplicated per-mode, matching how `--shadow-glow-focus` itself is a single mode-invariant definition) and mapped `--shadow-glow-focus-error: var(--shadow-glow-focus-error);` into `theme.css`'s `@theme inline` block next to `--shadow-glow-focus`. Not wired into any component — per the task, that's Phase 2 work.
  - **`bg/error-solid-active` — NOT added.** Figma's "Status Colors" section (node `6180:9680`, checked directly via `get_design_context` on the Error group `6180:9686`) has exactly 3 error swatches — `fg/error` (`#dc3a3a`), `bg/error` (`#fdeded`, a subtle/light fill, not a solid one), `border/error` (`#dc3a3a`) — with no hover/active/pressed variant of any kind, and (per the existing OPEN QUESTIONS entries from earlier units) there is no full Error primitive ramp in this file to derive a "one step darker" value from either (only Brand/Neutral/Success have complete 50–950 ramps). Brand's own active step (`--bg-brand-solid-active`) is one/two ramp steps darker than its solid/hover — but the task explicitly warned not to assume that same offset applies without Figma confirmation, and here there's nothing to confirm it against at all. Left `--bg-error-solid`/`--bg-error-solid-hover` exactly as they were in both `tokens.css` blocks; did not add `--bg-error-solid-active` anywhere. Logged below rather than guessed.
- Build (`pnpm build`) and test (`pnpm test`, 306 registry tests + 17 CLI tests) both pass with these changes. Committed `packages/registry/styles/tokens.css`, `packages/registry/styles/theme.css`, `packages/registry/ui/modal/modal.tsx`, and `packages/registry/ui/dialog/dialog.tsx`.
- **`radius/2xl` NOT added — it does not exist in Figma. No code, token, or CLAUDE.md changes made this unit.** Task: add a 7th radius step, `radius/2xl`, to `tokens.css`/`theme.css`, on the premise that Figma's radius scale has 7 steps vs. code's 6. Loaded `figma-design-to-code`, then located the canonical radius foundation directly: `get_metadata` on canvas `2066:14532` ("↳ Spacing, radius & grids", reached by walking sibling IDs near the already-confirmed canonical Colors frame `2066:14533`) returns exactly 3 foundation frames — "Foundations / Spacing" (`2066:16797`), **"Foundations / Radius" (`2066:16903`)**, and "Foundations / Grid Layouts" (`2066:16952`) — this appears to be the sole such page (no duplicate/legacy Radius frame found elsewhere, consistent with how the Colors canonical page was previously confirmed singular). `search_design_system` for "radius/2xl" / "radius" (entity: variable and component) returned zero local hits — only noise from unrelated external community libraries ("Website/Kiosk/Mobile Design System"), matching every prior unit's finding that this tool cannot see the file's own local, unpublished frames; had to go through `get_metadata` on the known node instead.
  - The "Radius Scale Section" of `2066:16903` lists **exactly 7 token cards, in order: `radius/0` (0px), `radius/xs` (6px), `radius/sm` (8px), `radius/md` (10px), `radius/lg` (14px), `radius/xl` (20px), `radius/full` (9999px)**. That is the file's real 7-step scale — the 7th, missing-from-code step is **`radius/0` (0px), not `radius/2xl`**. There is no `radius/2xl` (or any 2xl-named) token anywhere in this frame, and no other Radius/Depth & Shape frame exists in the file to check as an alternate source. The task's premise (that the missing 7th step is specifically named `2xl`) does not match what Figma actually contains.
  - Per the "never invent a value, stop and ask rather than guess" rule (which the task text itself extends to radius steps, since CLAUDE.md's Radius line is treated as effectively locked): did not fabricate a `--radius-2xl` value with no source, and did not add `--radius-0` either even though it's the real missing step — that's a different, unrequested token than what this unit was scoped to, and adding it unprompted felt like scope creep for a unit specifically framed around "the missing `radius/2xl` token." Left `tokens.css`, `theme.css`, and `CLAUDE.md`'s Radius line completely untouched.
  - **Confirmed (per task step 2) the other 6 steps have NOT drifted**: `xs=6px, sm=8px, md=10px, lg=14px, xl=20px, full=9999px` in Figma's Radius Scale Section match code and CLAUDE.md exactly, byte-for-byte. No drift to report on raw pixel values.
  - **Did find a real, separate, and significant drift** while reading this frame's "Usage Guidelines" section (component-radius-usage mapping, not a pixel value) — logged under OPEN QUESTIONS below rather than acted on, since it's unrelated to the 2xl ask and directly touches CLAUDE.md's locked-feeling Radius usage notes.
  - No component changes made (task step 4 doesn't apply — there is no 2xl token to point any component at). No build/test re-run needed since zero files changed; `git status` before and after this unit is identical (clean). Nothing committed for this unit beyond this log entry.
- **No z-index/elevation token scale added, and no z-index migration done — Figma has no Elevation/Z-index frame anywhere in the file. No code, token, or CLAUDE.md changes made this unit.** Task: build a 9-token z-index scale (`hide`, `base`, `dropdown`, `sticky`, `overlay`, `modal`, `popover`, `toast`, `tooltip`) from a real Figma spec and migrate every hardcoded `z-*` Tailwind utility in `packages/registry/ui/**` to it. Loaded `figma-design-to-code`, then crawled `get_metadata` from the canonical Foundations area of file `CDgfoMkj7lP3pXWJ3aOgkH` per this unit's specific pointer (canvas `2066:14532` and the canonical Colors frame `2066:14533`'s siblings).
  - First confirmed canvas `2066:14532` ("↳ Spacing, radius & grids") contains exactly the 3 frames prior units already found — "Foundations / Spacing" (`2066:16797`), "Foundations / Radius" (`2066:16903`), "Foundations / Grid Layouts" (`2066:16952`) — no 4th, Elevation/Z-index sibling frame exists there.
  - The top-level `get_metadata` (no nodeId) page listing only ever returns the single "🖼️ Cover" page (`117:22194`), which is itself a dead end (just cover art, no table of contents) — this tool appears not to enumerate this file's real page list, so every other canvas had to be found by walking sibling node IDs directly, same workaround prior units used for Colors/Spacing/Radius.
  - Found the real canvas ID for "Colors" is `2066:14526` (not `2066:14533`, which is the single content frame *inside* it — querying `2066:14533` directly returns just that frame with no visible siblings, which is why earlier units' shorthand of calling it "the Colors canvas" was imprecise but not wrong in effect). Canvas `2066:14526` contains only two frames: "Foundations / Colors" (`2066:14533`) and a "Success Usage Examples" demo frame (`6428:27341`) — no Elevation/Z-index sibling there either.
  - Walked the full Foundations page range by ID to be thorough: `2066:14525` is a bare section-divider canvas named "❖ FOUNDATIONS" (marks the start of the Foundations group in the pages panel, no content); `2066:14526` = Colors; `2066:14527` = not found (deleted/never-existed node); `2066:14528` = Typography (confirmed via its own frame, `2066:15667` — type scale only, no elevation content); `2066:14529` = Logos; `2066:14530` = not found (deleted/never-existed node); `2066:14531` = "Effect styles" canvas, containing "Foundations / Depth & Shape" (`2066:16312`, radius + shadow tiers), "Foundations / Shadows" (`2066:16436`, the full light/dark shadow-card reference including `glow-focus`), and "Foundations / Blurs" (`2066:16517`) — shadows/blurs/depth are covered here in detail, but there is no z-index/stacking-order content anywhere in this canvas either; `2066:14532` = Spacing/Radius/Grid Layouts (checked above). Tried several IDs beyond `14532` (`14533`–`14537`, `14540`, `14550`) and below `14526` (`14522`–`14524`) looking for a page that might have been added later or out of the main sequence — all either don't exist or resolve to content already accounted for above. That accounts for every Foundations canvas in the file (Colors, Typography, Logos, Depth & Shape/Shadows/Blurs, Spacing/Radius/Grid Layouts) with two ID slots (`14527`, `14530`) pointing at nodes that no longer exist — i.e., if an Elevation/Z-index page ever existed, it's one of those two now-deleted slots, not something still reachable today.
  - Per the ground rule and this task's own explicit fallback ("if the Figma frame doesn't exist, don't invent values — log it and leave the raw z-* utilities as they are"): made zero edits. Confirmed (via `grep`) the current raw usages this task named are all still exactly as described and untouched: `packages/registry/ui/avatar/avatar.tsx:25` (`z-10`, status dot), `packages/registry/ui/dialog/dialog.tsx:20,24` (`z-50` overlay + content), `packages/registry/ui/modal/modal.tsx:11,47` (`z-50` content + overlay), `packages/registry/ui/dropdown-menu/dropdown-menu.tsx:29` (`z-50`), `packages/registry/ui/select/select.tsx:92` (`z-50`), `packages/registry/ui/toast/toast.tsx:153` (`z-50`), plus two the task didn't explicitly name but that exist in the same codebase and would need the same token treatment once real values exist: `packages/registry/ui/popover/popover.tsx:33` (`z-50`) and `packages/registry/ui/tooltip/tooltip.tsx:28` (`z-50`). No files changed, so no build/test re-run needed; `git status` before and after this unit is identical (clean). Nothing committed for this unit beyond this log entry. See OPEN QUESTIONS.
- **No Motion foundation exists in Figma at all — the 11-token motion scale task (6 durations + easings) could not be sourced and was skipped entirely. No code, token, or CLAUDE.md changes made this unit.** Task: build `--duration-*`/`--ease-*` tokens (`duration/instant,fast,normal,moderate,slow,slower` + `easing/linear,ease-in,ease-out,ease-in-out`) from a real Figma spec and migrate the ad hoc hardcoded durations found via `grep -rn "duration-\|transition" packages/registry/ui/` — confirmed usages: `packages/registry/ui/accordion/accordion.tsx:57` (`transition-transform duration-200`, chevron rotation), `packages/registry/ui/progress-circle/progress-circle.tsx:79` (`transition-[stroke-dashoffset] duration-300`), `packages/registry/ui/progress-bar/progress-bar.tsx:59` (`transition-[width]`, no explicit duration), `packages/registry/ui/slider/slider.tsx:71` (`transition-[width,height]`, no explicit duration), plus unscoped `transition-colors` (relying on Tailwind's default duration, no explicit class) on `tabs.tsx`, `pagination.tsx` (×2), `input.tsx`, `toast.tsx`, `dialog.tsx` (×2), `textarea.tsx`, `button.tsx`, `avatar.tsx`, `select.tsx`, `breadcrumbs.tsx`.
  - Loaded `figma-design-to-code`, then crawled `get_metadata` from every known Foundations canvas in file `CDgfoMkj7lP3pXWJ3aOgkH` per this unit's specific pointers: canonical Foundations canvas `2066:14532` ("↳ Spacing, radius & grids" — contains exactly 3 sibling frames, "Foundations / Spacing" `2066:16797`, "Foundations / Radius" `2066:16903`, "Foundations / Grid Layouts" `2066:16952`, confirmed by a full metadata dump of the canvas — no 4th sibling frame, and no Motion content past the end of Grid Layouts, `2066:16952`/`2066:16958`, the IDs immediately after those explicitly checked and containing only Grid Layouts' own internal children, not a new sibling canvas/frame); the "Effect styles" canvas `2066:14531` (contains "Foundations / Depth & Shape" `2066:16312`, "Foundations / Shadows" `2066:16436`, and "Foundations / Blurs" `2066:16517` — checked this one specifically in case Motion was grouped inside it per the task's suggestion; it is not — no Motion/Animation/Transition section anywhere in its frame tree); Typography canvas `2066:14528` (type scale only); Logos canvas `2066:14529` (icon/wordmark symbols only). Canvases `2066:14527` and `2066:14530` re-confirmed "not found" (deleted), consistent with every prior unit tonight. The top-level `get_metadata` (no nodeId) call again only surfaces the single "🖼️ Cover" page (`117:22194`), which contains only cover art and no table of contents — re-confirmed this is a dead end for discovering additional pages, same finding as the z-index unit.
  - Also tried `get_variable_defs` (in case motion durations exist as Figma variables rather than a visual frame) on nodes `2066:14532` and `2066:14531` — both errored with "nothing selected," meaning this tool requires an active Figma-desktop selection rather than working against an arbitrary remote node id in this environment, so it could not be used to check for a variable-only motion collection. Not treated as evidence either way, just an unusable avenue.
  - Net result: every Foundations canvas in the file has now been walked (Colors, Typography, Logos, Depth & Shape/Shadows/Blurs, Spacing/Radius/Grid Layouts) and none contain any duration, easing, transition, or animation content. Per the task's explicit instruction ("if genuinely cannot find a Motion frame after a real search, do NOT invent duration/easing values — log it... and leave the codebase's current ad hoc duration-200/duration-300 hardcodes as they are"): made zero edits to `tokens.css`, `theme.css`, or any component file. `git status` before and after this unit is identical (clean). Nothing committed for this unit beyond this log entry. See OPEN QUESTIONS.

- **No Opacity foundation exists in Figma at all — the 8-token opacity scale task (0/5/10/20/40/60/80/100%) could not be sourced and was skipped entirely. No code, token, or CLAUDE.md changes made this unit.** Task: add `--opacity-0` … `--opacity-100` to `tokens.css`/`theme.css` and migrate the confirmed ad hoc opacity usages (`disabled:opacity-100`, `disabled:opacity-50`, `data-[disabled]:opacity-50`) to the semantically-correct named token, plus consider refactoring `--bg-overlay`'s baked-in 60% alpha to compose from an opacity token if that could be done cleanly.
  - Loaded `figma-design-to-code`, then checked the "Effect styles" canvas (`2066:14531`, file `CDgfoMkj7lP3pXWJ3aOgkH`) first per this unit's specific pointer, via `get_metadata`. This canvas contains exactly three frames: **"Foundations / Depth & Shape"** (`2066:16312` — Radius Scale, "Shadows - Light", "Shadows - Dark", and "Background Blur" sections), **"Foundations / Shadows"** (`2066:16436` — Shadows Light/Dark shadow cards including `glow-focus`, plus a note that shadow colors are hardcoded since Figma doesn't support variable-bound effect colors), and **"Foundations / Blurs"** (`2066:16517` — blur demos). Read every section header and sample card in all three frames end to end (Radius Scale, both Shadow tiers light+dark, Background Blur, both Shadow-card frames light+dark incl. glow-focus, Blur Demos) — no "Opacity" section, token card, or percentage-scale content anywhere in this canvas. The only opacity-adjacent numbers present are shadow-alpha percentages baked into individual shadow effect definitions (e.g. "0 1 2 0 · 6%" for shadow/xs) and blur px values — neither is a general-purpose opacity scale.
  - Also re-checked the "Foundations / Colors" frame (`2066:14533`) in full (all 8 top-level sections: Brand Palette, Neutral Palette, Success Palette, Semantic Tokens — Foreground/Background/Border, Status Colors, Dark Mode) specifically for an opacity value living alongside a semantic token (since `bg/overlay`'s own description text says "modal backgrounds at 60% opacity" and `fg/disabled`/`bg/disabled`/`border/disabled` are disabled-state tokens that might carry a percentage). None of the disabled-state tokens are opacity-based — they're all distinct solid hex colors (e.g. `fg/disabled` is its own gray step, not "fg/primary at X% opacity"), and `bg/overlay`'s 60% is a one-off baked into that single token's description, not a reference to a shared opacity scale.
  - This exhausts every Foundations canvas confirmed to exist in the file by this and prior units tonight (Colors `2066:14526`, Typography `2066:14528`, Logos `2066:14529`, Effect styles `2066:14531`, Spacing/Radius/Grid Layouts `2066:14532`), plus the two dead node slots (`2066:14527`, `2066:14530`) that resolve to "not found." No sixth canvas or hidden sibling was found.
  - Per the ground rule ("if genuinely cannot find an Opacity frame after a real search, do NOT invent an opacity scale — log it and leave every existing ad hoc opacity usage untouched"): made zero edits. Confirmed via `grep -rn "opacity-" packages/registry/ui/` that the existing ad hoc usages are exactly as described and untouched: `accordion.tsx:17` and `slider.tsx:54` (`data-[disabled]:opacity-50`), `slider.tsx:73` and `toggle-group.tsx:76` (`disabled:opacity-50`), `button.tsx:10` and `switch.tsx:30` (`disabled:opacity-100`). `--bg-overlay` in `tokens.css` (both light and dark blocks) also left untouched — its `rgb(16 24 40 / 0.6)` alpha stays inline since there is no opacity token to compose it from.
  - No files changed, so no build/test re-run needed; `git status` before and after this unit is identical (clean). Nothing committed for this unit beyond this log entry. See OPEN QUESTIONS.

- **Grid/breakpoint tokens added — 4 of 5 requested breakpoints exist in Figma (no `2xl`); columns and mobile/tablet margins added as documented, consumer-less `tokens.css` values.** Task: add `breakpoint/sm,md,lg,xl,2xl` (5), `columns/mobile,tablet,desktop` (3), `margin/mobile,tablet` (2). Loaded `figma-design-to-code`, then read the previously-confirmed "Foundations / Grid Layouts" frame (`2066:16952`, canvas `2066:14532`, file `CDgfoMkj7lP3pXWJ3aOgkH`) directly — `get_metadata` first, then `get_design_context` on its "Container Widths" sub-section (`2066:16988`) to confirm exact values.
  - **Breakpoints: only 4 exist, not 5.** The frame's 3 responsive-grid sections (Desktop `2066:16961`, Tablet `2066:16970`, Mobile `2066:16979`) plus its separate "Container Widths" section (`2066:16988`, confirmed via `get_design_context`) together define exactly `container/sm · 640px`, `container/md · 768px`, `container/lg · 1024px`, `container/xl · 1280px` — **no `2xl` container or breakpoint anywhere in this frame**, and no other frame in the file was found (by prior units tonight) to hold one either. All 4 values are byte-identical to Tailwind v4's own built-in default breakpoints. Added `--breakpoint-sm: 640px`, `--breakpoint-md: 768px`, `--breakpoint-lg: 1024px`, `--breakpoint-xl: 1280px` to a new block at the end of `packages/registry/styles/theme.css`'s plain `@theme { }` block (not `@theme inline` — these are static values, not references to a `tokens.css` runtime variable, matching how the existing `--text-*`/`--font-sans` entries in that same block are structured). Confirmed via Tailwind v4 docs/convention that `--breakpoint-*` is the correct CSS-first namespace for custom `sm:`/`md:`/etc. prefixes. Values are in plain `px` (not Tailwind's own default `rem`) to match this codebase's existing convention of literal px for spacing/radius (`tokens.css`) — a valid CSS length works identically either way in a `@media` query, so this doesn't change behavior. Verified the one existing bare `sm:` usage in the codebase (`packages/registry/ui/toast/toast.tsx:153`) is unaffected: since our explicit `--breakpoint-sm: 640px` matches Tailwind's own implicit default exactly, that utility now resolves to an explicit, intentional token instead of an implicit default, with no visual change.
  - **Columns: all 3 confirmed, added as-is.** Desktop/Tablet/Mobile sections' own annotation text give `Columns: 12` (desktop, 1024+), `Columns: 8` (tablet, 768–1023), `Columns: 4` (mobile, <640) — added to `packages/registry/styles/tokens.css`'s primitives block (next to `--radius-*`, since these are single-mode layout values with no light/dark variant) as `--grid-columns-mobile: 4`, `--grid-columns-tablet: 8`, `--grid-columns-desktop: 12`.
  - **Margins: both requested values confirmed, added; desktop's margin and all 3 gutters were also visible but out of this unit's requested scope, so left out (not a gap, a scope boundary).** Same annotations give `Margin: 16px` (mobile), `Margin: 24px` (tablet), and — not requested this unit — `Margin: 32px` (desktop) plus `Gutter: 24px/16px/12px` (desktop/tablet/mobile). Added only the 2 requested: `--grid-margin-mobile: 16px`, `--grid-margin-tablet: 24px` in `tokens.css`. Noted the unused desktop-margin/gutter values in a code comment so a future, properly-scoped unit doesn't have to re-read the frame from scratch.
  - Per the task's own instruction (these are layout/grid concepts, not broadly-reused single values like colors/spacing), both new token groups are added as documented values with **no current consumer** — confirmed (per an earlier phase of this run) that zero application patterns/grid-layout components exist yet in `packages/registry`. This is expected, not a defect.
  - Updated `CLAUDE.md`'s Design Tokens section: added a "Breakpoints/Grid (added 2026-09-11)" line directly after the existing Density line, stating the 4 confirmed breakpoint values, the "no 2xl" gap, and the column/margin values with their no-consumer status.
  - `pnpm build` and `pnpm test` both pass after these changes (registry build emits 33 components cleanly; 306 registry tests + CLI build pass, no new failures). Committed `packages/registry/styles/theme.css`, `packages/registry/styles/tokens.css`, and `CLAUDE.md`.

## Phase 2 — Components

- **Icon: sizing conventions confirmed, no wrapper component built (see rationale) — no commit.** Task explicitly warned against building a `name`-prop Icon wrapper unprompted (would mean refactoring every existing `lucide-react` consumer). No dedicated "Icon" component or a standalone "Foundations / Icon sizing" frame was found in Figma (consistent with every prior unit's finding that this file's Icons foundation, per CLAUDE.md's status line, lives as symbol-level sizing baked into each component's own spec, not a separate sizing-convention page). Instead, spot-checked the sizing convention already recorded in this repo's own build history (`BUILD_LOG.md`) against the current code: Button's confirmed Figma spec is 16px icon at sm/md, 20px at lg/xl (`packages/registry/ui/button/button.tsx` — `iconSizeClassName = size === "lg" || size === "xl" ? "size-5" : "size-4"`, matches exactly); Input's confirmed Figma spec (re-pulled 2026-08-25, node `6198:22642`) is 16px at sm, 20px at md/lg/xl (`packages/registry/ui/input/input.tsx`'s `iconSizeClassName` record — `sm: "size-4", md/lg/xl: "size-5"`, matches exactly). Spot-checked two more for the same 16/20px convention: Alert's icon (no size variants, single `size-5`/20px, correct for a non-density component) and Breadcrumbs' separator chevron (`size-4`/16px, correct for a compact inline glyph). All four confirm the same underlying convention (16px for compact/inline icons, 20px for standard interactive-control icon slots) with zero drift. No code changes; no commit.
- **Kbd — confirmed single-style component; no size/variant prop needed.** Pulled the real spec via `get_metadata` on canvas `2169:19985` (file `CDgfoMkj7lP3pXWJ3aOgkH`) since `get_design_context` errored in this session ("nothing selected" — same known limitation recorded in `BUILD_LOG.md` for earlier units; fell back to `get_metadata`/`get_screenshot` per that established precedent). Figma's "Published Components" section publishes exactly one `Kbd` symbol (72×26) built from one `_Kbd key` sub-symbol (30×26, correctly un-exported already — matches the `_`-prefix-stays-internal rule, though this repo's `Kbd`/`KbdGroup` don't literally use a `_KbdKey` sub-component, they achieve the same visual result inline) — no size axis, no second published variant. The doc's own "Properties" text does say `Variant: Light | Dark`, but a screenshot of the "Published Components" region (`6084:37300`) shows only a single rendering (⌘ + K, one style) — no second Light/Dark instance anywhere on the canvas. This is the same "documentation prose describes something the actual symbol set doesn't back up" pattern already seen for Divider's "configurable thickness" claim in a prior session (see `BUILD_LOG.md`) — concluded this refers to the component correctly adapting to the site's light/dark mode automatically via semantic tokens (already true: `bg-bg-secondary`/`border-border-default`/`text-fg-secondary` all theme-switch via CSS variables), not a discrete `variant` prop to add. Token bindings confirmed clean — `kbd.tsx` uses only `border-border-default`, `bg-bg-secondary`, `text-fg-secondary`, `text-ui-xs`, no hardcoded hex/px anywhere. No code changes; no commit.
- **Divider — orientation confirmed exact match; found a real spec/code gap on the label/icon front, logged rather than built (see OPEN QUESTIONS).** Pulled canvas `2120:12` via `get_metadata` (same `get_design_context` fallback as Kbd). Figma's "Published Components" publishes exactly `Orientation=Horizontal` and `Orientation=Vertical` — matches the code's `orientation` prop exactly, byte for byte. However the doc's "Properties" text reads `Orientation: Horizontal | Vertical` **and** `Variant: Plain | With Label | With Icon` — a `Variant` axis the current code only half-implements (an optional `label` prop covers "With Label"; there is no icon support at all for "With Icon"). Checked the "Unpublished Base Components" frame (`6391:27463`) and its screenshot directly: it shows only a 2-item "All orientations" grid (Horizontal, Vertical) — no Label or Icon variant instances anywhere, meaning the actual built symbol set has no Variant axis at all; "Variant: Plain | With Label | With Icon" is documentation prose unbacked by real symbols, the same pattern flagged for this exact component's "configurable thickness" claim in a prior session's audit. Left the existing `label` prop as-is (a pre-existing, reasonable code-level translation of "With Label" intent, not this unit's decision to make or unmake) and did **not** add icon support — there is zero visual/spec data anywhere in Figma to derive icon placement, size, or spacing from, so building it now would be pure invention. Token bindings confirmed clean (`border-border-default`, `text-fg-tertiary`, `bg-border-default`, all semantic). No code changes; no commit. See OPEN QUESTIONS.
- **Spinner — sizes confirmed exact match (16/24/32px); token bindings clean; found a real visual-construction mismatch, deliberately left unfixed as out of this unit's scope (see OPEN QUESTIONS).** Pulled canvas `2120:11` via `get_metadata` + screenshots (same fallback). Figma's "Published Components" publishes `Size=sm` (16×16), `Size=md` (24×24), `Size=lg` (32×32), and the Properties text confirms "Size: sm (16px) | md (24px) | lg (32px)" — matches `packages/registry/ui/spinner/spinner.tsx`'s `spinnerVariants` (`sm: size-4`/16px, `md: size-6`/24px, `lg: size-8`/32px) exactly, no drift. Token bindings are already fully semantic (`border-border-default` for the ring, `bg-fg-brand` for the highlight) — no hardcoded hex/px found. Separately, while screenshotting the sizes and in-context demos (`_loading-card`, `_inline-spinner`, `_button-loading`), noticed Figma's actual spinner renders as a ring with one **arc segment** highlighted in brand color (a conic/partial-border look, and the Properties text separately says it "inherits foreground color from parent context" — i.e. `currentColor`-driven, not a fixed brand color), whereas the current code builds it as a full gray ring plus a separate small **dot** element pinned to one corner, always brand-colored regardless of context. This is a real, visible construction difference, not a size or token-naming issue — but this unit's task was explicitly scoped to "verify token bindings and real Figma pixel sizes," and rebuilding the whole visual mechanic (ring+dot → ring+arc, plus switching brand-fixed color to `currentColor`-inherited) is a design-level change well beyond a token/size fix, and this run's ground rules forbid unrelated refactors. Left the component exactly as-is. See OPEN QUESTIONS.
- **Skeleton — renamed `variant="rect"` to `variant="rectangle"` to match Figma's real, confirmed variant name. Breaking prop-value change, per this unit's explicit instruction.** Pulled canvas `2120:13` via `get_metadata` (same fallback). Figma's "Published Components" section publishes the symbol **`Variant=Rectangle`** (not `Rect`) alongside `Variant=Text` and `Variant=Circle`, and the doc's own Properties text agrees exactly: "Variant: Text | Circle | Rectangle." This confirms the task's premise — the code's `"rect"` value was an abbreviation that doesn't match Figma's real (unabbreviated) variant-property value. Renamed across every file that references it: `packages/registry/ui/skeleton/skeleton.tsx` (the `cva` variant key and its default-export type), `packages/registry/ui/skeleton/skeleton.test.tsx` (2 usages), and the two docs-site consumers `apps/www/components/docs/skeleton/skeleton-demos.tsx` and `apps/www/components/docs/skeleton/skeleton-props-table.tsx` (both the live-preview code sample and the props-table type string). `registry.json`'s Skeleton description already said "rectangle" in prose, so it needed no change. No other consumers found (`grep` for `"rect"` across `apps/www`/`packages` returns nothing after the edit). Sizes/defaults were re-confirmed unchanged from the prior session's audit (Text's `h-4 w-full` default is a real canonical size per Figma's own demo; Circle/Rectangle's Figma demo dimensions — 48px, 200×120 — are illustrative examples, not canonical sizes, so both correctly stay unsized-by-default). `pnpm build` and `pnpm test` both pass (33 registry components built; 306 registry tests, including the renamed Skeleton tests, all green). Committed `packages/registry/ui/skeleton/skeleton.tsx`, `packages/registry/ui/skeleton/skeleton.test.tsx`, `apps/www/components/docs/skeleton/skeleton-demos.tsx`, `apps/www/components/docs/skeleton/skeleton-props-table.tsx` (commit `be5b0ee`).
- **Badge — audited against the real published component set; existing code already matches the confirmed spec exactly. No functional code change; no commit beyond this log entry.** `get_design_context` errored as expected ("nothing selected," documented limitation); fell back to `get_metadata` + `get_screenshot` on canvas `2120:3` ("↳ Badge"), specifically the "Published Components" frame `2121:14770`, which enumerates all **60 real symbols**: a compound **`Variant`** property with **15 values** — every one of Gray/Brand/Success/Warning/Error crossed with **Subtle, Outline, and Solid** (all three fills are real, confirmed published symbols; the written prose spec handed to this unit, which only mentioned "Subtle and Solid," was simply incomplete/wrong — Figma's actual file has always shipped Outline too, so the code's existing `outline` fill value is not "extra beyond spec," it's independently confirmed correct) — × **`Size`** (`sm`, `md` only — no `lg`, see open question below) × **`Type`** (`Label`, `Icon` only — **not** "Label, Icon, Dismiss" as the prose claimed; there is no third Type value anywhere in the 60 symbols). `Type=Icon` maps 1:1 to the code's existing `iconOnly` boolean (icon-only, no text) and `Type=Label` to `iconOnly=false` — this is a cleaner, equally-correct translation of a 2-valued enum into a boolean, so no rename was made. "Dismiss" isn't a `Type` value at all; it's a separate boolean toggle demonstrated in the canvas's "Configs" grid and "Demo → Section: Slots" rows, exactly matching the code's existing independent `dismissible` prop — already correct. No `Info` variant exists anywhere in the 60 symbols (only the 5 already coded) — no addition needed, and no `--bg-info-*`/`--fg-info`/`--border-info` tokens are needed on Badge. Visually cross-checked the full "All Variants" screenshot: Subtle (tinted bg + colored border + colored text), Outline (transparent bg + colored border + colored text), and Solid (colored bg + on-color text, **except Gray Solid, which Figma itself renders as a light-gray bg + dark text rather than an inverted fill** — confirms the code's deliberate special-case comment "gray has no dedicated solid token, uses tertiary bg") all match `badge.tsx`'s existing `compoundVariants` token-for-token, no drift found. The dismiss button's `focus-visible:shadow-[var(--shadow-glow-focus)]` was already present and correct — confirmed against CLAUDE.md's non-negotiable focus-ring rule, no change needed. Net result: the current `packages/registry/ui/badge/badge.tsx` fully and accurately reflects the real, confirmed Figma spec as-is. `pnpm build` (33 components) and `pnpm test` (306 registry + 17 CLI) both pass, unaffected since nothing changed. See OPEN QUESTIONS for two things surfaced but deliberately not acted on (ambiguous slot-demo evidence; a size-count doc/symbol mismatch).

## Phase 3 — Application Patterns

## Phase 4 — Final Pass

## OPEN QUESTIONS

Items logged here need the user's judgment call, not an autonomous guess.

- **🔴 PRIORITY — Brand/Gray/Success primitives: Figma shows different values than the locked brand identity. A change was made, then reverted; needs your explicit call before anyone touches color tokens again.** Figma's current "Foundations / Colors" page (`2066:14533`, cross-checked as the sole canonical page, no legacy duplicate) gives `brand/500 = #4469E5` (code/CLAUDE.md says `#3B63F5`), a full re-derived brand ramp (`brand-600` would become `#2E4DD9`, not the locked `#2450EA`), `gray-200`/`gray-800` one step off, and `success-600` off by a few points. Two independent swatches on that same Figma page agree with each other on the brand value, so this doesn't look like a fluke misread. **But `CLAUDE.md` explicitly locks `brand-600 = #2450EA` as "do not deviate without being told,"** the outcome of a previously-recorded, deliberate correction (see `BUILD_LOG.md`). An autonomous pass changed `tokens.css`/`CLAUDE.md` to match Figma's new numbers, built and tested clean, and committed (`aee1e6f`) — then that commit was reverted (`344b794`) before the rest of tonight's work continued, specifically because a value locked that explicitly shouldn't be overwritten without you seeing it first, no matter how well-sourced the replacement is. **You need to decide:** (a) Figma genuinely has a newer, intentional brand color and code should catch up (re-apply the reverted commit — it's still in git history at `aee1e6f`, fully built/tested, ready to cherry-pick), or (b) Figma has drifted by accident (someone nudged a swatch) and needs fixing on the Figma side instead, in which case code should stay exactly as it is. Nothing else in tonight's run depends on this — everything else was built against the current, locked `#2450EA` — but if you pick (a), the docs-site hardcoded color copies noted in the Phase 1 log (colors.mdx, foundation-scales.tsx, avatar-demos.tsx, opengraph-image.tsx) and the Phase 1 log's own now-reverted investigation notes would need to be re-applied/reconciled too.
- **No Error or Warning primitive (50–950) ramp exists in Figma — `--error-*`/`--warning-*` beyond 50/600/950 are unverified holdovers.** Confirmed (independently, walking the full canonical "Foundations / Colors" frame `2066:14533`) that only Brand, Neutral/Gray, and Success have dedicated 11-step ramp frames in this file; Error and Warning only appear as single semantic swatches in "Status Colors" (`fg/error = #DC3A3A`, `fg/warning = #DC9517`), which don't correspond cleanly to any one ramp step and aren't reliable enough to derive a full scale from (see next item). Left `tokens.css`'s `--error-*`/`--warning-*` values completely untouched rather than guess. Needs a human call: either (a) a designer adds real Error/Warning primitive-ramp frames to Figma (mirroring Brand/Neutral/Success), after which a future sync unit can verify the remaining steps, or (b) confirm the current code values are already correct/final and this note can be dropped.
- **The "Status Colors" semantic panel disagrees with the primitive ramp it should represent, even for Success (the one ramp we can check).** On the same canonical page, "Status Colors → Success" gives `fg/success = #249B5B`, but the "Success Palette" primitive ramp's `success/600` is `#058550` (and old code had `#079455`) — three different values for what should arguably be the same color. This means the Status Colors panel can't be trusted as a proxy for primitive-ramp values generally (it's presumably an older/illustrative panel that wasn't updated when Success Palette was last revised), which is part of why the Error/Warning swatches above weren't used to infer anything about a full ramp. Worth a human pass to decide whether Status Colors should be regenerated from the current primitives, independent of the Error/Warning ramp question above.
- **Info aliases `brand-600`, but its own code comment says it should match `brand-500`.** From the previous unit's work (kept as-is this unit, not modified): `--fg-info`, `--bg-info-solid`, and `--border-info` in `tokens.css` are bound to `var(--brand-600)`, but the comment directly above them says info's Figma swatches "resolve to the exact same hex values as brand/500 and brand/50." Confirmed this unit that `brand/500` (not `600`) is indeed the exact match (`#4469E5` for both `brand/500` and `fg/info`/`border/info`; `brand/50` for `bg/info`/`bg-info-subtle`). Left the binding unchanged since re-pointing it is a semantic-token decision (which step Info should alias), not a primitive-value fix, and out of scope for a tokens-only unit — but it means today `fg-info`/`border-info`/`bg-info-solid` render one shade darker (brand-600) than the comment claims they should. A human should decide whether to repoint those three to `--brand-500` to match the comment/Figma exactly, or update the comment to say `brand-600` is the intentional choice.
- **No dedicated "info" primitive color ramp exists in Figma.** Searched file `CDgfoMkj7lP3pXWJ3aOgkH`: `search_design_system` (entity: variable) for "info", "info/500", "info/600" returned nothing from this file's own design system (only unrelated community-library hits for "info" icon components); `get_metadata` crawled from the sole top-level page ("Cover", 117:22194) down through the "Foundations / Colors" frame (2066:14533) and enumerated every child frame — only Brand Palette (6180:1277), Neutral/Gray Palette (6180:1325), and Success Palette (6428:27293) have full 50–950 primitive swatch sets; there is no "Info Palette" or "Error Palette"/"Warning Palette" frame at all. The only Info-related content is a 3-swatch group (fg/info, bg/info, border/info) in the "Status Colors" section (node 6180:9720), and those 3 hexes are byte-identical to Figma's own Brand/500 (`#4469e5`) and Brand/50 (`#f0f4fe`) swatches — meaning Info reuses the Brand ramp by design in this file, it isn't a separate scale with 11 undiscovered steps. Rather than fabricate 9 intermediate `--info-100`…`--info-900`/`--info-950` values with no source, I skipped adding an `--info-*` primitive ramp to `tokens.css` entirely and instead aliased the 5 requested semantic tokens directly to the existing `--brand-*` primitives (see Phase 1 entry above). Needs a human call: (a) is Info intentionally a permanent Brand alias (in which case this is fully resolved, no further action needed), or (b) should a designer add a real, distinct Info ramp to Figma at some point, at which point the semantic tokens would get repointed from `--brand-*` to new `--info-*` primitives.
- **`bg/error-solid-active` requested this unit, but Figma has no data for it at all — not added, needs a designer to define it.** Task asked for the "pressed/active state of a solid-error button/badge," matching the shape of `--bg-brand-solid`/`-hover`/`-active`. Figma's "Status Colors" panel (node `6180:9680`) only has single fg/bg/border swatches for Error (`fg/error` and `border/error` = `#dc3a3a`, `bg/error` = `#fdeded` — the last being a subtle/light fill, not even the solid one our `--bg-error-solid` represents), with zero hover or active variants shown anywhere for Error. There's also no full Error primitive ramp in this file (confirmed again this unit, consistent with the "No Error or Warning primitive ramp exists" entry above) to derive a darker step from the way Brand's active step derives from its own ramp. This is a genuine gap, not a case where the existing brand offset pattern can be safely assumed — left `--bg-error-solid`/`--bg-error-solid-hover` untouched and did not add `--bg-error-solid-active`. Needs either: (a) a designer adds an explicit Error-solid hover/active pair to Figma's Status Colors panel (mirroring how Brand's solid/hover/active are all shown), or (b) an explicit instruction for which offset/value to use in its absence.
- **Confirmed (again) this file's own local design assets are invisible to `search_design_system`.** While looking for an error-tinted focus-ring effect style, `search_design_system` (entity: style, query "focus glow effect") returned only unrelated hits from external community libraries ("Website Design System," "Kiosk Design System," "Mobile Design System") — none from this file's own local styles/components. This matches the earlier "info" search finding (community-library noise, no local hits) and confirms `search_design_system` only indexes published-library assets, not this file's own local/unpublished frames — so any future search for a local component or effect style needs to go through `get_metadata`/`get_design_context` on a known node id instead, not `search_design_system`.
- **`radius/2xl` does not exist in Figma — the task premise was wrong; the real missing 7th radius step is `radius/0` (0px).** Figma's canonical "Foundations / Radius" frame (`2066:16903`, on canvas `2066:14532`, file `CDgfoMkj7lP3pXWJ3aOgkH`) lists exactly 7 radius tokens in its "Radius Scale Section": `radius/0` (0px), `radius/xs` (6px), `radius/sm` (8px), `radius/md` (10px), `radius/lg` (14px), `radius/xl` (20px), `radius/full` (9999px). Code has 6 of these (missing `radius/0`, not `radius/2xl`) — there is no token named or valued as a "2xl" step anywhere in this frame, and no second Radius/Depth-&-Shape frame exists elsewhere in the file to check as an alternate source. Nothing was added (neither a fabricated `--radius-2xl` nor the real-but-unrequested `--radius-0`) — see the matching Phase 1 entry above for the full trail. **Needs a human call:** (a) confirm whether "2xl" was simply a wrong assumption in tonight's task list (in which case this is resolved — no action needed, drop the request), or (b) if a `radius/2xl` step is genuinely wanted, have a designer add it to the Figma "Foundations / Radius" frame first (it isn't there today), and note separately whether `radius/0` (the step that actually exists but isn't in code) should also be picked up in a future, properly-scoped unit.
- **🔴 Figma's Radius "Usage Guidelines" now show the exact mapping that was deliberately corrected away from on 2026-08-23 — a locked, documented usage note may need re-litigating.** While reading the "Foundations / Radius" frame (`2066:16903`) for the 2xl investigation above, its "Usage Guidelines" section (node `6180:1229`) currently reads: `radius/md` (10px) → "Inputs, buttons, and small interactive surfaces"; `radius/lg` (14px) → "Cards, panels, and elevated content containers"; `radius/xl` (20px) → "Modals, large dialogs, and full-screen overlays"; `radius/full` → "Pills, avatars, and circular icons." But `CLAUDE.md`'s current Radius line says the opposite for two of these — `radius-sm` (8px) for buttons/inputs, `radius-lg` (14px) for modals, `radius-xl` "unused so far" — and explicitly states this was "Corrected 2026-08-23 against the real Figma specs... the previous '(inputs/buttons)' on `radius-md` and '(modals)' on `radius-xl` didn't match what Figma actually specifies (buttons/inputs are `radius-sm`; modals are `radius-lg`)." In other words, Figma's live Usage Guidelines today show precisely the pre-2026-08-23 mapping that was found wrong and corrected away from — either Figma has drifted back/been edited since that correction, or the 2026-08-23 correction misread the frame at the time. I did **not** touch any component's radius class or `CLAUDE.md`'s Radius line over this — per the task's own instruction, an existing, documented radius-usage mapping is "effectively as locked as colors," and this is exactly the kind of non-additive, cascades-into-multiple-components conflict (Button, Input, Modal, Card would all be affected) that should be escalated rather than silently re-flipped a second time. **Needs a human decision**: which mapping is actually current/intentional, followed by a dedicated Phase 2 unit to re-audit Button/Input/Modal/Card's actual radius classes against whichever mapping is confirmed correct.
- **No Elevation/Z-index foundation exists in Figma at all — the 9-token z-index scale task (`hide`/`base`/`dropdown`/`sticky`/`overlay`/`modal`/`popover`/`toast`/`tooltip`) could not be sourced and was skipped entirely.** Exhaustively walked every Foundations canvas in file `CDgfoMkj7lP3pXWJ3aOgkH` (Colors `2066:14526`, Typography `2066:14528`, Logos `2066:14529`, Effect styles/Depth & Shape/Shadows/Blurs `2066:14531`, Spacing/Radius/Grid Layouts `2066:14532`) plus every frame inside each — none contain any elevation, stacking-order, or z-index content. Two canvas-ID slots in the Foundations range, `2066:14527` and `2066:14530`, resolve to "node not found" (i.e., a page was created there and later deleted) — if an Elevation page ever existed, it's most likely one of these two, now gone. Full detail and the exact IDs checked are in the matching Phase 1 log entry above. Per this task's own explicit instruction, no values were invented and no components were migrated — all current raw `z-10`/`z-50` Tailwind utilities across `avatar`, `dialog`, `modal`, `dropdown-menu`, `select`, `toast`, `popover`, and `tooltip` remain exactly as they were. **Needs a human call**: (a) a designer adds a real "Foundations / Elevation" (or similarly named) frame to Figma defining the 9 real z-index values, after which a future sync unit can build the token scale and do the migration in one pass as originally scoped, or (b) if this file never had such a frame and the two deleted-node IDs are unrelated, confirm there's no elevation spec to recover and provide the 9 real values directly (e.g., via a design review) so a future unit isn't stuck re-walking the same dead ends.
- **No Motion/Animation foundation exists in Figma at all — the 11-token motion scale task (6 durations + easings, e.g. `duration/instant,fast,normal,moderate,slow,slower` and `easing/linear,ease-in,ease-out,ease-in-out`) could not be sourced and was skipped entirely.** Walked every Foundations canvas in file `CDgfoMkj7lP3pXWJ3aOgkH` — Colors `2066:14526`, Typography `2066:14528`, Logos `2066:14529`, "Effect styles" canvas `2066:14531` (Depth & Shape `2066:16312`, Shadows `2066:16436`, Blurs `2066:16517` — checked specifically in case Motion was grouped in here per the task's suggestion; it is not), and `2066:14532` (Spacing `2066:16797`, Radius `2066:16903`, Grid Layouts `2066:16952` — confirmed exactly 3 sibling frames, no 4th Motion sibling, and no content past Grid Layouts' own end). `2066:14527`/`2066:14530` re-confirmed deleted, same as every prior unit. The top-level `get_metadata` (no nodeId) call again only surfaces the single "🖼️ Cover" page with no table of contents — a dead end, consistent with the z-index unit's finding. Also tried `get_variable_defs` on `2066:14532`/`2066:14531` in case durations exist only as Figma variables (no visual frame needed) — both errored ("nothing selected"), since that tool needs an active Figma-desktop selection, not an arbitrary remote node id; inconclusive, not treated as evidence either way. This is the same dead-end pattern as the Elevation/Z-index search above — if a Motion page ever existed, it's most likely one of the same two deleted node slots (`14527`/`14530`), now gone. Per this task's explicit instruction, no duration/easing values were invented, no tokens were added to `tokens.css`/`theme.css`, and every existing ad hoc hardcode remains untouched: `packages/registry/ui/accordion/accordion.tsx:57` (`transition-transform duration-200`), `packages/registry/ui/progress-circle/progress-circle.tsx:79` (`transition-[stroke-dashoffset] duration-300`), `packages/registry/ui/progress-bar/progress-bar.tsx:59` and `packages/registry/ui/slider/slider.tsx:71` (unscoped `transition-[...]`, no explicit duration), and unscoped `transition-colors` across `tabs.tsx`, `pagination.tsx`, `input.tsx`, `toast.tsx`, `dialog.tsx`, `textarea.tsx`, `button.tsx`, `avatar.tsx`, `select.tsx`, `breadcrumbs.tsx`. **Needs a human call**: (a) a designer adds a real "Foundations / Motion" (or similarly named) frame to Figma defining the real duration/easing values, after which a future sync unit can build the token scale and do the migration in one pass as originally scoped, or (b) if this file never had such a frame, confirm there's no motion spec to recover and provide the 11 real values directly so a future unit isn't stuck re-walking the same dead ends.
- **No Opacity foundation exists in Figma at all — the 8-token opacity scale task (0/5/10/20/40/60/80/100%) could not be sourced and was skipped entirely.** Checked the "Effect styles" canvas (`2066:14531`) first as this unit's task specifically suggested (opacity often lives alongside shadows/blurs) — it contains exactly "Foundations / Depth & Shape" (`2066:16312`: Radius Scale, Shadows Light/Dark, Background Blur), "Foundations / Shadows" (`2066:16436`: shadow cards incl. glow-focus), and "Foundations / Blurs" (`2066:16517`: blur demos) — read in full, no Opacity section in any of the three. Also re-checked the full "Foundations / Colors" frame (`2066:14533`, all 8 sections) in case a disabled-state opacity value was documented alongside a semantic color token — `fg/disabled`/`bg/disabled`/`border/disabled` are each distinct solid hex colors, not "color at X% opacity," and `bg/overlay`'s "60% opacity" is a one-off baked into that single token's own description text, not a reference to any shared opacity scale. This, combined with prior units' exhaustive walks of every other Foundations canvas (Typography `2066:14528`, Logos `2066:14529`, Spacing/Radius/Grid Layouts `2066:14532`, plus dead node slots `2066:14527`/`2066:14530`), accounts for the entire Foundations area of the file — no Opacity frame exists anywhere. Per this task's explicit instruction, no opacity values were invented, no tokens were added to `tokens.css`/`theme.css`, and every existing ad hoc usage remains untouched: `packages/registry/ui/accordion/accordion.tsx:17` and `packages/registry/ui/slider/slider.tsx:54` (`data-[disabled]:opacity-50`), `packages/registry/ui/slider/slider.tsx:73` and `packages/registry/ui/toggle-group/toggle-group.tsx:76` (`disabled:opacity-50`), `packages/registry/ui/button/button.tsx:10` and `packages/registry/ui/switch/switch.tsx:30` (`disabled:opacity-100`); `--bg-overlay` in `tokens.css` also left as a hardcoded `rgb(... / 0.6)` since there's no opacity token to compose it from. **Needs a human call**: (a) a designer adds a real "Foundations / Opacity" frame to Figma defining the 8 real values, after which a future sync unit can build the token scale and do the migration in one pass as originally scoped, or (b) if this file never had such a frame, confirm there's no opacity spec to recover and provide the 8 real values directly so a future unit isn't stuck re-walking the same dead ends.
- **Divider's Figma doc text advertises a `Variant: Plain | With Label | With Icon` property, but the actual published symbol set has no Variant axis at all (only `Orientation=Horizontal`/`Vertical`) — "With Icon" has zero visual data to build from.** Canvas `2120:12`'s "Unpublished Base Components" frame (`6391:27463`) — the source symbols the published component is built from — only contains a 2-item "All orientations" grid (Horizontal, Vertical); no Label or Icon variant instance exists anywhere on the canvas to pull real spacing/sizing/icon-placement details from. The code's existing `label` prop already covers the "With Label" half reasonably (a pre-existing decision from an earlier session, not revisited here). Needs a human call: (a) a designer adds real "With Icon" symbol(s) to Figma's Divider base-components frame, after which a future unit can build it from a real reference, or (b) confirm "With Icon" was aspirational/never actually built and drop it from the doc text.
- **Spinner's Figma spec renders as a ring-with-highlighted-arc (`currentColor`-inherited per its own Properties text), but the code builds it as a full gray ring plus a separate small dot pinned to one corner, always fixed to `fg-brand` regardless of context.** Sizes and token bindings both check out exactly (16/24/32px; `border-border-default`/`bg-fg-brand`, no hardcodes) — this is a visual-construction and color-inheritance difference, not a size/token defect, surfaced incidentally while screenshotting Spinner's demos (`_loading-card`, `_inline-spinner`, `_button-loading`) for the size check. Left unchanged since this unit's task was scoped narrowly to sizes/tokens and rebuilding the whole rendering mechanic (dot → arc, brand-fixed → `currentColor`) is a real design change outside "touch only what this task requires." Needs a human call on whether to redo Spinner's visual construction to match Figma's arc/currentColor design in a dedicated future unit.
- **No `2xl` breakpoint exists in Figma's "Foundations / Grid Layouts" frame — only 4 of the 5 requested breakpoints (`sm`/`md`/`lg`/`xl`) could be sourced; `2xl` was not added.** Task asked for a 5-breakpoint scale (`sm,md,lg,xl,2xl`). Read the "Container Widths" section (`2066:16988`) of frame `2066:16952` (file `CDgfoMkj7lP3pXWJ3aOgkH`) directly via `get_design_context`, plus `get_metadata` on the whole frame — its 3 responsive-grid sections (Desktop/Tablet/Mobile) and its "Container Widths" section together define exactly 4 named breakpoints/containers (`sm` 640px, `md` 768px, `lg` 1024px, `xl` 1280px, all matching Tailwind v4's own built-in defaults) and nothing named or valued as `2xl` anywhere in the frame. Added `--breakpoint-sm/md/lg/xl` to `theme.css` (see matching Phase 1 log entry) but did not fabricate a `--breakpoint-2xl` (Tailwind's own implicit default for that step is `1536px`/`96rem`, which would have been an easy, plausible-looking guess — deliberately not used, since it's not confirmed anywhere in this Figma file). **Needs a human call**: (a) a designer adds a `2xl`/`container/2xl` entry to Figma's "Container Widths" section (most likely at `1536px`, mirroring Tailwind's own default, if that's the intended value), after which a future unit can add `--breakpoint-2xl` in one pass, or (b) confirm this design system intentionally caps at `xl` (1280px) with no `2xl` tier, in which case this note can be dropped and the task's premise of "5 breakpoints" corrected going forward.
- **Badge's canvas (`2120:3`) doc text says `Size: sm | md | lg`, but the actual 60 published symbols only instantiate `sm`/`md` — no `lg` symbol exists anywhere on the canvas.** Same "prose doc block disagrees with the real published symbol set" pattern already seen on Kbd/Divider in this file. Trusted the real instantiated symbols (the ground truth) over the doc prose and did not add an `lg` size to `badge.tsx`. Needs a human call: (a) a designer adds real `Size=lg` symbols for all 15 Variant values (and both Types) if `lg` is genuinely wanted, after which a future unit can build it from real reference, or (b) confirm the doc text is stale/aspirational and correct it to `sm | md` to stop this resurfacing.
- **Badge's "Demo → Section: Slots" rows (leading icon / leading dot / trailing icon / dismissible / icon + dismiss, canvas `2120:3`) show inconsistent, likely-stale demo content — not treated as a reliable spec for new `leadingIcon`/`trailingIcon`/`avatar` props.** Screenshotting each row individually: every single row (including "trailing icon," "leading dot," and even "icon + dismiss") renders the *same* trailing arrow "→" glyph, and the row explicitly labeled "dismissible" shows plain "Badge →" with **no dismiss ✕ at all** — directly contradicting the canvas's own Accessibility Note ("dismiss button role=button with aria-label=\"Remove\"") and the already-implemented, already-correct `dismissible` prop. This looks like stale/duplicated instance-override content left over from demo authoring, not a genuine "every Badge needs a persistent trailing-arrow slot" requirement. Separately, the "Unpublished Base Components" Configs grid (`6396:27652`) shows a "+ Leading avatar" toggle that never appears in the actual "Published Components" frame (`2121:14770`) or in the Demo section at all — and that frame is explicitly labeled "Unpublished base components — edit these to update the main published Badge," i.e. it's marked as not-yet-shipped. Per this run's "log genuine ambiguity, don't guess" rule, no `leadingIcon`, `trailingIcon`, or `avatar` prop was added to `badge.tsx`. Needs a human call: (a) if leading-icon/trailing-icon/leading-avatar slots are real, wanted features, have a designer clean up the Demo section with correct, distinguishable per-row content (and publish "Leading avatar" if it's meant to ship) so a future unit can build from unambiguous reference, or (b) confirm these are not real requirements and the demo section should be corrected/trimmed to stop this resurfacing.
