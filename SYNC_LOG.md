# Design System Sync Log

Autonomous overnight sync of `packages/registry` against the Figma source of truth (file `CDgfoMkj7lP3pXWJ3aOgkH`), started 2026-09-11. One line (or a few) per completed unit of work, in commit order. See the plan this run followed for full context.

_Summary block will be filled in at the top of this file once the final pass completes._

## Noted for later (not tonight)

- Docs-site UI redesign (new "Docs · Desktop" Figma frame — top bar, sidebar nav, live-preview panels, props tables, pagination footer) — explicitly deferred to a separate, visually-reviewed session. When it happens: the frame's sample code (`@paubha/react`, `PaubhaProvider`) is illustrative, not literal — adapt to the real CLI install flow (`npx paubha init` / `add button`), no importable package or provider component unless separately requested.

## Phase 1 — Tokens

- **Info semantic tokens added (no dedicated primitive ramp — see OPEN QUESTIONS).** Pulled Foundations/Colors from Figma (`CDgfoMkj7lP3pXWJ3aOgkH`, frame "Foundations / Colors" 2066:14533 → "Status Colors" section, node 6180:9680) via `get_design_context`. Figma's "Status Colors" panel gives only 3 raw swatches for Info (fg/info, bg/info, border/info) — no 50–950 primitive ramp exists for Info in this file (only Brand, Neutral/Gray, and Success have full ramps). The 3 Info swatch hexes resolved to `#4469e5` (fg/info, border/info) and `#f0f4fe` (bg/info) — these are exactly Figma's own Brand/500 and Brand/50 swatch values (confirmed by cross-checking the "Brand Palette" frame, node 6180:1277), i.e. Figma's real design intent is that Info deliberately aliases the Brand hue rather than getting its own ramp. Added to `packages/registry/styles/tokens.css`: semantic tokens `--bg-info-solid`, `--bg-info-subtle`, `--fg-info`, `--fg-on-info`, `--border-info` in both the light (`:root`) and dark (`.dark`/`[data-preview-theme="dark"]`) blocks, bound via `var()` to the existing `--brand-*` primitives following the exact same relationship the codebase already uses for `--fg-brand`/`--border-brand`/`--bg-brand-subtle` (brand-600/brand-50 light, brand-400/brand-950 dark; `--fg-on-info` mirrors `--fg-on-brand`/`--fg-on-success` as `--base-white` in both modes). No new hex values were introduced — every info token is a `var()` alias of an already-confirmed real primitive. Mirrored the same 5 tokens into `packages/registry/styles/theme.css`'s `@theme inline` block (`--color-bg-info-solid`, `--color-bg-info-subtle`, `--color-fg-info`, `--color-fg-on-info`, `--color-border-info`), matching the existing warning/success mapping pattern. Did not add a primitive-layer `--color-info`/`--color-info-subtle` short alias (like `--color-error`/`--color-warning`/`--color-success`) since there is no `--info-*` primitive scale to alias.
- **Alert/Toast "info" variant wired to the new tokens (one-line swap each).** `packages/registry/ui/alert/alert.tsx`: `info` variant class changed from `"border-border-brand bg-bg-brand-subtle text-fg-brand"` to `"border-border-info bg-bg-info-subtle text-fg-info"` — functionally identical result today (since info tokens alias brand), but now uses the dedicated info semantic layer, matching how the `warning`/`success`/`error` cases already reference their own dedicated tokens. `packages/registry/ui/toast/toast.tsx`: `info` accent-bar class changed from `"bg-bg-brand-solid"` to `"bg-bg-info-solid"`, mirroring the `success`/`warning`/`error` cases exactly. Both are pure token-reference swaps — no markup, props, or variants changed. `pnpm build` and `pnpm test` both pass (306 registry tests + 17 CLI tests, no new failures) after these changes.

- **Brand ramp re-synced (full 11 steps) — resolved the brand/500 discrepancy flagged for this unit.** Investigated file `CDgfoMkj7lP3pXWJ3aOgkH` directly: `get_metadata`/`get_design_context` on the "Foundations / Colors" frame (`2066:14533`) returned the complete page in one subtree (height 4376 accounted for exactly by its children — header, Brand Palette, Neutral Palette, Success Palette, 3 Semantic Tokens sections, Status Colors, Dark Mode, footer), with no second/legacy "Brand Palette" or "Colors" frame anywhere else in the file. This is also the exact node `tokens.css`'s own header comment cites as its source, so it's the canonical page, not a stale duplicate. Its "Brand Palette" frame (`6180:1277`) reads `brand/500 = #4469E5`, matching the "Status Colors → Info" swatch (`fg/info`/`border/info` = `#4469E5`, `bg/info` = `#F0F4FE` = `brand/50`) on the *same* page — two independent swatches on one canonical frame agreeing exactly, which is as strong a confirmation as this task can get without a human. This is a real, additional drift beyond the documented 2026-08-22 correction (old code `brand-600 = #2450EA` "true blue" vs. this file's current `brand-600 = #2E4DD9`) — not a misread of a legacy frame and not the old pre-2026-08-22 indigo (`#4658DE`) reappearing; it's a third, distinct ramp that Figma has since moved to. Updated all 11 `--brand-*` steps in `packages/registry/styles/tokens.css` to match Figma exactly (50 `#F0F4FE` → 950 `#1A2150`), recomputed the two shadow-effect blocks that hardcode brand's RGB decomposition since plain CSS can't derive `rgb()` components from a hex custom property (`--shadow-xs..2xl`: brand-900 `rgb(30 52 133/…)` → `rgb(36 51 128/…)`; `--shadow-glow-focus`: brand-500 `rgb(59 99 245/…)` → `rgb(68 105 229/…)`), and updated `CLAUDE.md`'s brand-color callout and Design Tokens table to match, with a "Corrected 2026-09-11" note explaining the supersession. All other semantic/component code is `var()`-bound and picks this up automatically — no component files touched.
- **Gray (Neutral) and Success primitives spot-corrected — 3 total steps.** Same canonical page. "Neutral Palette" (`6180:1325`): 9 of 11 steps already matched code exactly; `neutral/200 = #EAECF0` (code had `#E4E7EC`) and `neutral/800 = #182230` (code had `#1D2939`) did not — updated `--gray-200` and `--gray-800` in `tokens.css` and `CLAUDE.md` to match. "Success Palette" (`6428:27293`): 10 of 11 steps matched; `success/600 = #058550` (code had `#079455`) did not — updated `--success-600` in both files. No structural changes (no tokens added/removed), values only, per scope.
- **Error and Warning primitive ramps: confirmed (again, independently) that no such frame exists in Figma — left untouched.** Walked the entire canonical "Foundations / Colors" frame (`2066:14533`) and its full child list (see above) — there is no "Error Palette" or "Warning Palette" 50–950 frame anywhere on this page, matching the previous unit's finding for Info. The only Error/Warning data in Figma is one semantic swatch each in "Status Colors" (`fg/error` = `#DC3A3A`, `fg/warning` = `#DC9517`), and these don't cleanly correspond to any single ramp step — worth noting, the Status Colors panel's `fg/success` swatch (`#249B5B`) doesn't even match the *current* Success Palette ramp's `success/600` (`#058550`) or the old code value (`#079455`) on this same page, which undermines treating Status Colors as authoritative for any full ramp. Left `--error-*` and `--warning-*` entirely as-is (not even the single semantic-swatch values), per the "leave code as-is rather than guess" rule. See OPEN QUESTIONS.
- **Resolved the "info" question from the previous unit: `#4469e5` was real and current, not a misread.** Confirmed above — it is Figma's live, current `brand/500`, verified via two independent swatches on the single canonical page. The previous agent's info-token work (aliasing `--fg-info`/`--bg-info-solid`/`--border-info`/etc. to the brand ramp rather than fabricating a 9-step info scale) remains the right call and needed no rework; it now automatically resolves to the corrected brand values via `var()`. One follow-up worth flagging: the previous unit's own code comment says info's swatches "resolve to the exact same hex values as brand/500 and brand/50," but the actual bindings use `var(--brand-600)` for `--fg-info`/`--bg-info-solid`/`--border-info` (not `--brand-500`) — i.e. the comment and the binding disagree on which brand step Info aliases. I did not change this binding myself since it's a semantic-layer design decision (which step to alias to), not a primitive-value fix, and outside this unit's scope — logged under OPEN QUESTIONS instead.
- Build (`pnpm build`) and test (`pnpm test`, 306 registry tests + 17 CLI tests) both pass with these changes. Committed `packages/registry/styles/tokens.css` and `CLAUDE.md` only (`apps/www/public/r/*.json` is a gitignored build artifact, regenerated automatically and not hand-edited). Docs-site files that hardcode literal copies of these hexes for illustration (`apps/www/content/docs/foundations/colors.mdx`, `apps/www/components/docs/_shared/foundation-scales.tsx`, `apps/www/components/docs/avatar/avatar-demos.tsx`, `apps/www/app/opengraph-image.tsx`) were intentionally left untouched — they're not token-bound, updating them is a docs-site content pass outside "touch only what this task requires" for a tokens-only unit, and doing so would have meant editing several component/content files beyond `tokens.css`/`CLAUDE.md`. Flagging so a later docs-sync phase picks them up — they'll show stale brand/gray/success swatches until then.

## Phase 2 — Components

## Phase 3 — Application Patterns

## Phase 4 — Final Pass

## OPEN QUESTIONS

Items logged here need the user's judgment call, not an autonomous guess.

- **No Error or Warning primitive (50–950) ramp exists in Figma — `--error-*`/`--warning-*` beyond 50/600/950 are unverified holdovers.** Confirmed (independently, walking the full canonical "Foundations / Colors" frame `2066:14533`) that only Brand, Neutral/Gray, and Success have dedicated 11-step ramp frames in this file; Error and Warning only appear as single semantic swatches in "Status Colors" (`fg/error = #DC3A3A`, `fg/warning = #DC9517`), which don't correspond cleanly to any one ramp step and aren't reliable enough to derive a full scale from (see next item). Left `tokens.css`'s `--error-*`/`--warning-*` values completely untouched rather than guess. Needs a human call: either (a) a designer adds real Error/Warning primitive-ramp frames to Figma (mirroring Brand/Neutral/Success), after which a future sync unit can verify the remaining steps, or (b) confirm the current code values are already correct/final and this note can be dropped.
- **The "Status Colors" semantic panel disagrees with the primitive ramp it should represent, even for Success (the one ramp we can check).** On the same canonical page, "Status Colors → Success" gives `fg/success = #249B5B`, but the "Success Palette" primitive ramp's `success/600` is `#058550` (and old code had `#079455`) — three different values for what should arguably be the same color. This means the Status Colors panel can't be trusted as a proxy for primitive-ramp values generally (it's presumably an older/illustrative panel that wasn't updated when Success Palette was last revised), which is part of why the Error/Warning swatches above weren't used to infer anything about a full ramp. Worth a human pass to decide whether Status Colors should be regenerated from the current primitives, independent of the Error/Warning ramp question above.
- **Info aliases `brand-600`, but its own code comment says it should match `brand-500`.** From the previous unit's work (kept as-is this unit, not modified): `--fg-info`, `--bg-info-solid`, and `--border-info` in `tokens.css` are bound to `var(--brand-600)`, but the comment directly above them says info's Figma swatches "resolve to the exact same hex values as brand/500 and brand/50." Confirmed this unit that `brand/500` (not `600`) is indeed the exact match (`#4469E5` for both `brand/500` and `fg/info`/`border/info`; `brand/50` for `bg/info`/`bg-info-subtle`). Left the binding unchanged since re-pointing it is a semantic-token decision (which step Info should alias), not a primitive-value fix, and out of scope for a tokens-only unit — but it means today `fg-info`/`border-info`/`bg-info-solid` render one shade darker (brand-600) than the comment claims they should. A human should decide whether to repoint those three to `--brand-500` to match the comment/Figma exactly, or update the comment to say `brand-600` is the intentional choice.
- **No dedicated "info" primitive color ramp exists in Figma.** Searched file `CDgfoMkj7lP3pXWJ3aOgkH`: `search_design_system` (entity: variable) for "info", "info/500", "info/600" returned nothing from this file's own design system (only unrelated community-library hits for "info" icon components); `get_metadata` crawled from the sole top-level page ("Cover", 117:22194) down through the "Foundations / Colors" frame (2066:14533) and enumerated every child frame — only Brand Palette (6180:1277), Neutral/Gray Palette (6180:1325), and Success Palette (6428:27293) have full 50–950 primitive swatch sets; there is no "Info Palette" or "Error Palette"/"Warning Palette" frame at all. The only Info-related content is a 3-swatch group (fg/info, bg/info, border/info) in the "Status Colors" section (node 6180:9720), and those 3 hexes are byte-identical to Figma's own Brand/500 (`#4469e5`) and Brand/50 (`#f0f4fe`) swatches — meaning Info reuses the Brand ramp by design in this file, it isn't a separate scale with 11 undiscovered steps. Rather than fabricate 9 intermediate `--info-100`…`--info-900`/`--info-950` values with no source, I skipped adding an `--info-*` primitive ramp to `tokens.css` entirely and instead aliased the 5 requested semantic tokens directly to the existing `--brand-*` primitives (see Phase 1 entry above). Needs a human call: (a) is Info intentionally a permanent Brand alias (in which case this is fully resolved, no further action needed), or (b) should a designer add a real, distinct Info ramp to Figma at some point, at which point the semantic tokens would get repointed from `--brand-*` to new `--info-*` primitives.
